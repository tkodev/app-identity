import { NextPage } from 'next'

type HomePageProps = {}

const HomePage: NextPage<HomePageProps> = (props) => {
  const {} = props

  return (
    <div>
      <h1>HomePage</h1>
    </div>
  )
}

export default HomePage
