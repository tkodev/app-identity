box: kroniak/ssh-client
build:
  steps:
    - add-ssh-key:
      keyname: ssh_key
    - add-to-known_hosts:
      hostname: www.htko.ca
    - script:
      name: connect to www.htko.ca
      code: ssh ${ssh_user}@www.htko.ca ''cd /srv/projects/code/htko/site-base && git pull''
